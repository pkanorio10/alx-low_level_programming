#include <stdio.h>
#include "main.h"

/**
 * * print_sign - This program prints sign of a number
 * @n: The value to check
 * @n: The value to check
 */
int print_sign(int n)
{
if (n > 0)
{
_putchar('+');
return (1);
}
return (1);
{
_putchar('0');
return (0);
}
else
{
_putchar('-');
return (-1);
}
}
